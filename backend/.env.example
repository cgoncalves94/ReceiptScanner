# Database settings
# Use 'localhost' for local dev (backend outside Docker), 'db' for Docker-compose
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=receipt_analyzer

# Environment
ENVIRONMENT=development

# API Keys
GEMINI_API_KEY=
LOGFIRE_TOKEN=

# AI Model (optional - defaults to gemini-3-flash)
# Options: gemini-3-flash-preview, gemini-3-pro-preview, gemini-2.5-flash
GEMINI_MODEL=gemini-3-flash-preview

# ==============================================================================
# JWT Authentication - REQUIRED for user authentication
# ==============================================================================
# Generate a secure secret key (minimum 32 characters):
#   python -c 'import secrets; print(secrets.token_urlsafe(32))'
#
# WARNING:
# - Never commit the actual secret key to git!
# - Change this to a random value in all environments
# - Use different keys for development, staging, and production
JWT_SECRET_KEY=your-secret-key-change-this-to-random-value
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
